import type { ReactElement } from 'react';
import { DemoMetricsView } from '@/modules/Admin/presentation/DemoMetricsView';

export default function DemoMetricsPage(): ReactElement {
  return <DemoMetricsView />;
}
